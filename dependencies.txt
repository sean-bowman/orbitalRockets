# orbitalRockets -- Orbital-Class Launch Vehicle Engineering
#
# Install with:  pip install -r dependencies.txt
# Target runtime: Python 3.10 (win-amd64)
#
# This is the union across every domain. An individual domain that is being run standalone may
# carry its own narrower dependencies.txt.

# -- Core numerics -- #
numpy>=2.2
scipy>=1.15

# -- Fluid properties -- #
# fluidProps() in common/fluidProperties.py dispatches REFPROP first, then falls back to CoolProp.
# CoolProp alone is sufficient to run every tool here, with the exception of hydrazine, which has no
# equation of state in either backend and is served by the built-in correlation table instead.
CoolProp>=8.0
ctREFPROP>=0.10.5

# -- Plotting -- #
matplotlib>=3.10
plotly>=6.2

# -- Testing -- #
pytest>=8.0
